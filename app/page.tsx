import { FC } from 'react';
// import { CounterContainer } from './components/CounterContainer';
import EditorContainer from './components/WorkspaceContainer';

const Home: FC = () => {
    return (
        <main>
            
            <EditorContainer />
        </main>
    );
};

export default Home; 

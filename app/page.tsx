import { FC } from 'react';
import { CounterContainer } from './components/CounterContainer';

const Home: FC = () => {
    return (
        <main>
            <CounterContainer />
        </main>
    );
};

export default Home; 
